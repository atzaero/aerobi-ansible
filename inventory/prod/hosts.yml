---
# Inventário — Ambiente de Produção
#
# VPS principal onde as aplicações reais rodam.
# Configurações mais restritivas de segurança.

all:
  hosts:
    vps-prod:
      ansible_host: 187.127.6.20
      ansible_user: deploy
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519
