---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart docker-events-alerter
  ansible.builtin.systemd:
    name: "{{ docker_events_alerter_service_name }}"
    state: restarted
